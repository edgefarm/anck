apiVersion: network.edgefarm.io/v1alpha1
kind: Network
metadata:
  name: mystream-network
  namespace: stream
spec:
  namespace: stream
  accountname: streams-account
  participants: []
  streams:
    - name: myStream
      subjects:
        - mySubject1
      storage: file
      global: true
      public: true

  imports: []
