---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: networks.network.edgefarm.io
spec:
  group: network.edgefarm.io
  names:
    kind: Network
    listKind: NetworkList
    plural: networks
    singular: network
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Network is the Schema for the networks API
          properties:
            apiVersion:
              description:
                "APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
              type: string
            kind:
              description:
                "Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the client
                submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
              type: string
            metadata:
              type: object
            spec:
              description: NetworkSpec defines the desired state of Network
              properties:
                accountname:
                  description:
                    Accountname is the name of the nats account. If empty,
                    the namespace, where the Network ressource was deployed is used.
                  type: string
                namespace:
                  description:
                    Namespace is the namespace the credentials shall be stored
                    in. If empty, the accountname is used for credential deplyoment.
                  type: string
                usernames:
                  description: Usernames is the name of the nats users.
                  items:
                    type: string
                  type: array
              required:
                - usernames
              type: object
            status:
              description: NetworkStatus defines the observed state of Network
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
